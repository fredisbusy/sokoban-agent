# TODO

위에서 아래 순서로 진행한다. 항목은 완료 조건을 검증한 뒤에만 체크한다.

## 지금 — 환경 기반

- [ ] 타일, 행동, 레벨 형식을 정의한다.
- [ ] Gymnasium `reset`, `step`, `render`를 구현한다.
- [ ] 이동, 상자 밀기, 승리, 무효 이동, 기본 데드락을 판정한다.
- [ ] `reset/step/win/deadlock` 테스트를 작성한다.

완료 조건: 고정 1-box 퍼즐이 `level_id`로 재현되고 `pytest`, Ruff,
mypy가 통과한다. seed는 에이전트와 레벨 선택의 무작위성에 사용한다.

## 다음 — 기준선과 측정

- [ ] 공통 Agent 인터페이스와 Random Agent를 만든다.
- [ ] 비교용 BFS 또는 A* 솔버를 만든다.
- [ ] 성공률, 행동 수, 무효 이동, 데드락, 시간을 기록한다.
- [ ] 환경과 기준선 결과를 보여주는 첫 노트북을 만든다.

완료 조건: 같은 레벨 묶음에서 두 기준선을 반복 실행하고 결과를 비교한다.

## 이후 — LLM 에이전트

- [x] LiteLLM을 통해 Ollama 모델을 호출한다.
- [ ] 보드를 직렬화하고 행동 출력을 구조화한다.
- [ ] 빈 응답, 출력 형식 오류, 무효 행동에 재시도·복구 규칙을 둔다.
- [ ] 모델, seed, 행동 제한을 설정으로 고정하는 실행기를 만든다.
- [ ] LLM과 기준선의 성공률·효율·응답 시간을 비교한다.

완료 조건: 소형 고정/미지 레벨에서 반복 가능한 비교 결과를 남긴다.

## 보류

- [ ] 렌더링 화면을 읽는 perception을 추가한다.
- [ ] 방문한 보드 상태, 실패한 계획, 데드락을 누적하는 memory를 추가한다.
- [ ] 절차 생성 레벨 또는 Boxoban 벤치마크로 확장한다.
